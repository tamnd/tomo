# R2: prompt cache and request stability

These labs capture complete provider requests and measure the bytes and fields that remain stable across a growing real turn.
Live arms use only free Zen models and reject `gpt-*` model names.
