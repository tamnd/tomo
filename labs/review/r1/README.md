# R1: provider startup

These labs prove when tomo first contacts a model provider and how invalid provider configuration stops before contact.
They run the built command with an isolated home directory and observe network requests directly.
