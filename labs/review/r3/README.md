# R3: context growth, pruning, and compaction

These labs measure how complete model history grows, what remains present, and how compaction changes only the provider view.
Live arms use only free Zen models and reject `gpt-*` model names.
